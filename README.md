# Media Storage
A web app doing exactly as the title implies. Intended for image storage.

## Backend (Go)
```
go build ./cmd/main.go && ./main
```

## Frontend (React / Vite)
from "web" directory
```
npm run dev
```
